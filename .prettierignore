.astro
.vercel
.codex
node_modules
dist
coverage
pnpm-lock.yaml

# The markdown archive is prose. Prettier reflows it, which rewrites every
# post for no benefit — it has never been formatted and should not start.
src/content

# Generated narration manifests (scripts/generate-narrations.mjs)
public/narration

# Carried over from the Next codebase untouched; it is rewritten in the final
# migration phase, and reformatting it now would bury that diff in noise.
src/islands/BlogNarrator.tsx
